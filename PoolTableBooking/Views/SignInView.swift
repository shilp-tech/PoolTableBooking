// SignInView is no longer used — authentication is handled via Sign in with Apple in AuthView.
import SwiftUI

struct SignInView: View {
    var onShowCreateAccount: () -> Void
    var body: some View { EmptyView() }
}
